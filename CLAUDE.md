# 專案說明（給 Claude 的接續筆記）

台灣觀光旅館住房率分析與預測 — 資料科學期末專題 + 口試簡報。這份檔案是給任何一台電腦上的
新 Claude session 看的，讓它不用重問一輪就能接上進度。

## 溝通偏好

- **一律用繁體中文回覆**，不要用簡體字或英文作主要回覆語言。
- 精確、量化的溝通：講「改了什麼、數字從多少變多少」，不要用「應該有改善」這種模糊說法。
- 先抓根因、列出其他受影響的地方，再問要不要一起改——不要自作主張做要求以外的順手清理。
- 改完東西要自己驗證過（compile、跑一次、開起來看）才能說「改好了」，不要先講完成再讓使用者發現沒動。

## 目錄結構與 canonical 檔案

- `旅館分析-claude/build_deck_v2.py` —— **正式的 25 頁簡報生成器**（「價值重構版」），口試與期末
  報告都用這份。`旅館分析-claude/25頁建議版/`、`gemini/`、根目錄下的舊 25頁／27頁版本都是**淘汰版**，
  不要改到那些。
- `旅館分析-claude/build_oral.py` —— 另一份，5 分鐘口試專用的精簡版（13 頁）。
- `旅館分析-claude/build_report.py` —— 書面專題報告（Word .docx）生成器，內容取材自 25 頁簡報。
- `旅館分析-claude/build_deck.py` —— 共用基礎元件（`head/text/rect/chart/table/kpi/why/note` 等函式
  與顏色常數），被 `build_deck_v2.py`／`build_oral.py` 用 `exec()` 讀取其「投影片內容之前」的部分
  取用；它自己獨立執行時產出的是已淘汰的「25頁建議版」，不是交付物。
- `旅館分析-claude/compute_facts.py` —— **唯一數字來源**。所有簡報數字都從這裡算出寫入
  `旅館分析-claude/facts.json`，簡報生成器只讀這個檔案，不能在投影片裡寫死任何數字。改動模型或
  資料後，要重跑這個腳本，`facts.json` 才會更新。

## 目前模型狀態（2026-09-15 起）

模型已從「一次切分」改為**三段式時間切分**，修正了舊版「選模直接用了盲測集，回報值略樂觀」的
方法論弱點：

- 訓練 2023-01～2024-12（2,748 筆）→ 驗證選模 2025 全年（1,390 筆，選模全程不碰盲測資料）
  → 盲測 2026-01～06（694 筆，只算一次）。
- GB 超參數（n_estimators=250, max_depth=3, learning_rate=0.03）由驗證集網格搜尋選出。
- **盲測 MAE = 6.07pp**（改善 19.2%，優於最好的簡單基準；bootstrap 95% CI [1.06, 1.96]）——
  比舊版 6.08pp 略好，且方法論比舊版乾淨。
- 訓練腳本：`src/train_monthly_3way.py`；報表輸出在 `reports/*_3way.*`；模型檔
  `models/monthly_model_3way.pkl`。
- 特徵重要性同時保留內建重要度（`feature_importance_monthly_3way.csv`）與置換重要性
  （`permutation_importance_monthly_3way.csv`，`build_deck_v2.py` 第 21 頁主要敘事改用這個）。

## 兩個 Streamlit App（新舊並存，不要互相覆蓋）

- 根目錄 `app.py` + `pages/` —— **舊版**，標題「2023~2025訓練」，模型走
  `models/monthly_model_through_202606.pkl`（2-way 切分）。已部署在
  `taiwan-hotel-occupancy.streamlit.app`。
- `app_2023_2024/app.py` + `app_2023_2024/pages/` —— **新版**，標題「2023-2024訓練 2025測試」，
  模型走 `models/monthly_model_3way.pkl`（三段式切分）。`ROOT` 路徑比舊版多繞一層
  （`parent.parent[.parent]`），跟舊版共用同一份 `data/`、`models/`、`reports/`、`src/`，
  **不要複製資料進這個資料夾**。目前規劃另外拆一個獨立 GitHub repo
  （`taiwan-hotel-occupancy-2024`，尚未建立）給這個新版部署用，讓它在 GitHub 作品集裡是
  獨立的一個項目，不要跟舊版擠在同一個 repo 呈現。

## 旅館家數口徑（常被搞混，務必分清楚）

- **125** = `hotels_all`，整份資料集出現過的旅館數（含 2026 上半年）。
- **124** = `hotels_hist`，2023–2025 範圍內的旅館數，app.py 卡片用這個。
- **114** = 三年（2023、2024、2025）都在榜的平衡樣本（balanced panel）。
- **113** = 2025 全年 12 個月資料完整的旅館數。

## 其他已知事項

- 三份旅館名稱裡有房間數跨月不一致、造成規模分箱重複計數的問題（124+4=128），已在簡報第 16 頁
  加註揭露，不需要再「修正資料」，只要維持揭露即可。
- `旅館分析-claude/claude-25p/c/` 底下有兩份較早期、Claude 自己寫的探索版簡報（儀表板深化報告、
  產業經營數據分析報告），內容跟現版有重疊也有互補（Top10 排行、衰退觀察名單等現版沒有的素材），
  比較細節見已發布的 Artifact 對照表。
